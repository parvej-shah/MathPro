# Memory — Append-Only Execution Log

Continuity across sessions. **Append only.** Never rewrite or prune past entries
(`invariants.md` § Documentation). One entry per meaningful change.

Format: `YYYY-MM-DD — <what changed> | scope: <files/feature> | why: <reason>`

---

2026-06-13 — Established `.claude/` orchestration architecture (CLAUDE.md operating contract, invariants.md, conventions.md, this log, FEATURES/{course-details,courses-page}/{purpose,rules}.md). | scope: .claude/**, CLAUDE.md | why: prevent context decay + scope-creep refactors as the codebase grows; encode hard-won rules from context.md as enforced invariants.
2026-06-14 — Added homepage per-category tag filtering for featured courses using course tags (for labels like SSC/HSC sessions) and preserved the existing live course directory feed. | scope: src/components/landing-page.tsx, .claude/memory.md | why: let students filter homepage course cards by session-style tags such as SSC 27 / SSC 28 without changing the backend contract.
2026-06-14 — Switched `/courses` hero slider to backend-managed featured courses and consumed the new public featured-course feed. | scope: src/app/courses/page.tsx, src/features/courses-page/components/FeaturedCourseSlider.tsx, src/features/courses-page/hooks/{useCoursesPage,useFeaturedCourses}.ts, .claude/memory.md | why: let admin explicitly control which courses appear in the courses-page slider instead of relying on the first bundle/course in generic lists.
2026-06-14 — Hardened the shared navbar surface so non-landing pages keep an opaque background in production while preserving the landing-page transparent-at-top behavior. | scope: src/components/Nav.tsx, .claude/memory.md | why: prevent checkout/course pages from rendering a fully transparent navbar when the semi-transparent token class is not applied consistently in production.
2026-06-14 — Added `/dashboard/billing` payment-history page wired to the four previously-unused PaymentSummary/Transactions/Courses/Bundles components and a "পেমেন্ট হিস্টরি" nav link; de-duplicated 3 separate `/user/payment/history` fetches by having `useCoursesWithRanking` and the post-payment success page reuse `usePaymentHistory`. | scope: src/app/dashboard/billing/page.tsx, src/hooks/useCoursesWithRanking.ts, src/app/post-payment/success/page.tsx, src/components/Nav.tsx, .claude/memory.md | why: users previously had no UI to view purchase/payment history despite the backend and built components existing; also cuts redundant API calls.
2026-06-14 — Switched combo detail routing to prefer combo slugs and updated reusable combo links/SEO to use `bundle.url` when present. | scope: src/app/combos/[id]/page.tsx, src/components/BundleCard.tsx, .claude/memory.md | why: `/combos/test-combo` was calling the numeric-only bundle detail endpoint and failing even though combo cards already linked by slug.
2026-06-14 — Added a printable digital invoice feature: `src/components/Invoice/InvoiceDocument.tsx` (Bengali invoice layout, print-friendly via Tailwind `print:` variants) rendered at `/dashboard/billing/invoice/[transactionId]`, looked up from `usePaymentHistory().all_transactions`; added `coupon` field to `Transaction`/`IndividualCourse`/`BundlePurchase` types in `usePaymentHistory.ts` (backend already returns it, type was missing it); added "ইনভয়েস দেখো" link in `PaymentTransactions.tsx` (also translated its remaining English copy to Bengali) and "ইনভয়েস ডাউনলোড করো" link in `OrderSummary.tsx` on the post-payment success page. | scope: src/hooks/usePaymentHistory.ts, src/components/Invoice/InvoiceDocument.tsx (new), src/app/dashboard/billing/invoice/[transactionId]/page.tsx (new), src/components/PaymentTransactions.tsx, src/components/PostPayment/Success/OrderSummary.tsx | why: todo item 18 — users need a downloadable/printable receipt for enrollments, accessible from both the billing page and post-purchase success page; uses browser print-to-PDF (no new dependency, Bengali fonts render correctly as HTML/CSS).
2026-06-14 — Moved the billing/payment-history pages out of the dashboard route group: `src/app/dashboard/billing/page.tsx` → `src/app/billing/page.tsx`, `src/app/dashboard/billing/invoice/[transactionId]/page.tsx` → `src/app/billing/invoice/[transactionId]/page.tsx`. Updated all `/dashboard/billing` references to `/billing` in `Nav.tsx`, `PaymentTransactions.tsx`, `OrderSummary.tsx`, and the invoice page's internal back-links. | scope: src/app/billing/** (moved), src/components/Nav.tsx, src/components/PaymentTransactions.tsx, src/components/PostPayment/Success/OrderSummary.tsx | why: billing/payment-history is not part of the dashboard feature, so it gets its own top-level route.
2026-06-14 — Fixed the invoice print output including the whole page (global Nav + Footer) instead of just the invoice card: wrapped `<AppNavbar />` and `<Footer />` in root layout in `print:hidden` divs (both are positioned/structured independently of this wrapper, so it doesn't affect normal layout). | scope: src/app/layout.tsx | why: the invoice page's own `print:hidden` only covered its local action buttons — the site-wide nav/footer rendered by the root layout were still printing on every page, including `/billing/invoice/[transactionId]`.
2026-06-14 — Made course enrollment date cards span the full sidebar row when a row would otherwise contain a single card, and removed the unused `isPrebookingMode` prop from `EnrollmentInfo`. | scope: src/features/course-details/components/EnrollmentInfo.tsx, src/app/courses/[id]/page.tsx, .claude/memory.md | why: avoid the empty half-row gap on `/courses/[id]` when enrollment dates are odd in count.
2026-06-14 — Restyled the course countdown block with a modern glass card, tighter hierarchy, and responsive four-tile layout while removing hardcoded hex colors. | scope: src/features/course-details/components/CountdownTimer.tsx, .claude/memory.md | why: make the enrollment deadline timer feel more polished and aligned with the course details card aesthetic.
2026-06-14 — Renamed the study-plan tab/header to `সিলেবাস` and removed the stats strip from that section so the content starts directly with the chapter list. | scope: src/features/course-details/components/StudyPlanTab.tsx, src/features/course-details/components/CourseTabs.tsx, src/app/courses/[id]/page.tsx, .claude/memory.md | why: simplify the section header and match the requested syllabus presentation.
2026-06-14 — Changed the default selected tab on course details to Instructor instead of Syllabus. | scope: src/app/courses/[id]/page.tsx, .claude/memory.md | why: make the instructor content visible first on `/courses/[id]`.
2026-06-14 — Reworked the instructor tab into a compact scan-friendly list with smaller avatars, lighter cards, and no decorative background blob. | scope: src/features/course-details/components/InstructorTab.tsx, .claude/memory.md | why: make the instructor section easier to skim while keeping the same instructor data.
2026-06-14 — Revamped the `/billing` page UI: rewrote `PaymentSummary`, `PaymentTransactions`, `PaymentCourses`, `PaymentBundles` to use MathPro emerald-token theme (`bg-linear-to-r from-primary to-primary/85`, removed `bg-purple`/raw `bg-gradient-to-r`), full Bengali copy (incl. "কম্বো" for bundles per copy invariant), `englishToBanglaNumbers` for all counts/currency, and fixed course/combo card links to `/dashboard/{id}` + `/dashboard/bundle/{id}` (continue learning) and `/courses/{url||id}` + `/combos/{url||id}` (details) instead of the broken `/course/{id}` and `/bundle/{id}`. Also translated the billing page's error state to Bengali. | scope: src/components/PaymentSummary.tsx, src/components/PaymentTransactions.tsx, src/components/PaymentCourses.tsx, src/components/PaymentBundles.tsx, src/app/billing/page.tsx | why: previous billing page used English copy, hardcoded gradients/legacy `bg-purple` (forbidden token), low-contrast header text, and dead course/bundle links — didn't match MathPro's theme or invariants.
2026-06-14 — Replaced emoji icons in the billing page's PaymentSummary stat cards with `react-icons/bs` icons (`BsWallet2`, `BsBook`, `BsBoxSeam`, `BsArrowRepeat`) inside rounded `bg-primary/10` icon badges. | scope: src/components/PaymentSummary.tsx | why: emoji icons looked dated/inconsistent across platforms; matches the icon-badge pattern used elsewhere (e.g. Dashboard CourseCard via react-icons).

2026-06-14 — Removed the ASSIGNMENT module type from the student LMS (backend migration 014 dropped the assignment feature; quiz kept). Deleted the `useAssignment` hook; removed the assignment state/props/render block from `ModulePlayer` and its wiring + `useAssignment` call in the module `page.tsx` (incl. the goToModule ASSIGNMENT branch and stale `taken`/`fetchEvalutedAssignment` deps); dropped `"ASSIGNMENT"` from the `ModuleCategory` union (`_components/types.ts`) and the course-details category union (`_lib/types.ts`); removed the ASSIGNMENT icon/label in `ModuleListItem`, the ASSIGNMENT gating clause in `ModuleNavButtons`, the ASSIGNMENT icon+label branches in `features/course-details/ChapterAccordion`, and the unused `AssignmentSkeleton` (def + barrel re-export). `npx tsc --noEmit` clean. Deliberately LEFT untouched (out of scope, not assignment-authoring): the `notification.type === "ASSIGNMENT"` handling in NotificationItem/NotificationModal/NotificationsPageClient (notification feature, `type: string`), and the `countAssignmentsAndVideos`/`countModulesAssignmentsVideos` helpers (general counters; their `assignmentCount` is now always 0 and never displayed — removing would ripple return types for no behavioral gain). | scope: src/app/course/[courseId]/[chapterid]/[moduleid]/** , src/features/course-details/** , src/components/Skeletons/** | why: track backend migration 014 — assignment feature removed product-wide.

2026-06-14 — Phase 4 Live-Class toggle (Frontend). Added `live_status`/`live_meeting_id`/`live_meeting_pass`/`live_scheduled_at` to the module `CourseModule` type and a live banner in `ModulePlayer` shown when `live_status==='LIVE'` (Bengali copy: "লাইভ ক্লাস চলছে" + zoom meeting id/passcode/scheduled time). When the class ends (status ENDED / not live) the module plays as a normal video — the banner just disappears. Fields flow from backend `module.js` list/get `SELECT m.*` → course.getFull, no extra fetch. NOTE: pinning live modules to the public homepage is NOT done — `landing-page.tsx` is a static marketing mockup (its "LIVE" badge is decorative), so homepage live-pinning is a separate feature. | scope: src/app/course/[courseId]/[chapterid]/[moduleid]/_components/{types.ts,ModulePlayer.tsx} | why: enrolled students see zoom join info while a module is live; recording shows after.

2026-06-14 — Removed CODE module type (Frontend), tracking backend CODE removal. Dropped CODE from both category unions; removed the two CODE render paths in `ModulePlayer` (internal problem page + Codeforces verify) + the `cfHandle/setCfHandle/checkCFStatus` props (and orphaned `Link` import); removed the `checkCFStatus` callback + CF props in module `page.tsx`; removed CODE from `ModuleListItem` (label+icon), `ModuleNavButtons` gating, `features/course-details/ChapterAccordion` (icon+label), and the displayed "টি কোডিং চ্যালেঞ্জ" count chip in BOTH accordions; removed now-unused `is_cf`/`cf_name` from `ModuleData`. `npx tsc --noEmit` clean. LEFT untouched: helpers' `codeCount`/`totalCodes` tallies (always 0 now, general counters — same call as the assignment counts), and the Ranking "Codeforces অ্যাকাউন্ট" leaderboard column (separate feature, students' CF handles — NOT the CODE module type). | scope: course module player + features/course-details + helpers | why: CODE module type removed product-wide. |
2026-06-15 — Added a live-class widget to the main `/dashboard` page (above StatsSection). New hook `src/hooks/useMyLiveModules.ts` calls the new backend `GET /user/module/live` (aggregates `module.live_status IN ('SCHEDULED','LIVE')` across all enrolled courses); new component `src/components/Dashboard/LiveClassBanner.tsx` renders the first result as a featured card (red for LIVE, primary for SCHEDULED, Bengali copy "এখন লাইভ"/"আসন্ন লাইভ ক্লাস"), linking to `/dashboard/{course_id}`. Named `useMyLiveModules` (not `useLiveClasses`) to avoid collision with the existing per-course `useLiveClasses` hook, which targets the separate legacy `live` table via `/user/live/list/:id` (used by `LiveClassesSection` on the course dashboard) — these are two distinct live-class systems; this widget only covers the new `module.live_status` system. | scope: src/hooks/useMyLiveModules.ts (new), src/components/Dashboard/LiveClassBanner.tsx (new), src/app/dashboard/page.tsx | why: students asked to see/join the current or next live class right from the main dashboard.
2026-06-15 — Reworked LMS quiz behavior to use backend-authored quiz metadata: weighted scoring from per-question `points`, one-time final submission with no retake path, local restore of submitted answers/results, optional timer enforcement from `quiz_time_limit`, and markdown-image rendering so admin-uploaded quiz images display in the student player. | scope: src/app/course/[courseId]/[chapterid]/[moduleid]/**, src/components/SafeHtmlRenderer.tsx, .claude/memory.md | why: align the student quiz player with admin-authored marks/time rules and support image-based MCQ content without duplicate submissions.
