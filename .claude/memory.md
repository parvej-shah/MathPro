# Memory — Append-Only Execution Log

Continuity across sessions. **Append only.** Never rewrite or prune past entries
(`invariants.md` § Documentation). One entry per meaningful change.

Format: `YYYY-MM-DD — <what changed> | scope: <files/feature> | why: <reason>`

---

2026-06-13 — Established `.claude/` orchestration architecture (CLAUDE.md operating contract, invariants.md, conventions.md, this log, FEATURES/{course-details,courses-page}/{purpose,rules}.md). | scope: .claude/**, CLAUDE.md | why: prevent context decay + scope-creep refactors as the codebase grows; encode hard-won rules from context.md as enforced invariants.
2026-06-14 — Added homepage per-category tag filtering for featured courses using course tags (for labels like SSC/HSC sessions) and preserved the existing live course directory feed. | scope: src/components/landing-page.tsx, .claude/memory.md | why: let students filter homepage course cards by session-style tags such as SSC 27 / SSC 28 without changing the backend contract.
2026-06-14 — Switched `/courses` hero slider to backend-managed featured courses and consumed the new public featured-course feed. | scope: src/app/courses/page.tsx, src/features/courses-page/components/FeaturedCourseSlider.tsx, src/features/courses-page/hooks/{useCoursesPage,useFeaturedCourses}.ts, .claude/memory.md | why: let admin explicitly control which courses appear in the courses-page slider instead of relying on the first bundle/course in generic lists.
2026-06-14 — Hardened the shared navbar surface so non-landing pages keep an opaque background in production while preserving the landing-page transparent-at-top behavior. | scope: src/components/Nav.tsx, .claude/memory.md | why: prevent checkout/course pages from rendering a fully transparent navbar when the semi-transparent token class is not applied consistently in production.
2026-06-14 — Added `/dashboard/billing` payment-history page wired to the four previously-unused PaymentSummary/Transactions/Courses/Bundles components and a "পেমেন্ট হিস্টরি" nav link; de-duplicated 3 separate `/user/payment/history` fetches by having `useCoursesWithRanking` and the post-payment success page reuse `usePaymentHistory`. | scope: src/app/dashboard/billing/page.tsx, src/hooks/useCoursesWithRanking.ts, src/app/post-payment/success/page.tsx, src/components/Nav.tsx, .claude/memory.md | why: users previously had no UI to view purchase/payment history despite the backend and built components existing; also cuts redundant API calls.
2026-06-14 — Switched combo detail routing to prefer combo slugs and updated reusable combo links/SEO to use `bundle.url` when present. | scope: src/app/combos/[id]/page.tsx, src/components/BundleCard.tsx, .claude/memory.md | why: `/combos/test-combo` was calling the numeric-only bundle detail endpoint and failing even though combo cards already linked by slug.
2026-06-14 — Added a printable digital invoice feature: `src/components/Invoice/InvoiceDocument.tsx` (Bengali invoice layout, print-friendly via Tailwind `print:` variants) rendered at `/dashboard/billing/invoice/[transactionId]`, looked up from `usePaymentHistory().all_transactions`; added `coupon` field to `Transaction`/`IndividualCourse`/`BundlePurchase` types in `usePaymentHistory.ts` (backend already returns it, type was missing it); added "ইনভয়েস দেখো" link in `PaymentTransactions.tsx` (also translated its remaining English copy to Bengali) and "ইনভয়েস ডাউনলোড করো" link in `OrderSummary.tsx` on the post-payment success page. | scope: src/hooks/usePaymentHistory.ts, src/components/Invoice/InvoiceDocument.tsx (new), src/app/dashboard/billing/invoice/[transactionId]/page.tsx (new), src/components/PaymentTransactions.tsx, src/components/PostPayment/Success/OrderSummary.tsx | why: todo item 18 — users need a downloadable/printable receipt for enrollments, accessible from both the billing page and post-purchase success page; uses browser print-to-PDF (no new dependency, Bengali fonts render correctly as HTML/CSS).
2026-06-14 — Moved the billing/payment-history pages out of the dashboard route group: `src/app/dashboard/billing/page.tsx` → `src/app/billing/page.tsx`, `src/app/dashboard/billing/invoice/[transactionId]/page.tsx` → `src/app/billing/invoice/[transactionId]/page.tsx`. Updated all `/dashboard/billing` references to `/billing` in `Nav.tsx`, `PaymentTransactions.tsx`, `OrderSummary.tsx`, and the invoice page's internal back-links. | scope: src/app/billing/** (moved), src/components/Nav.tsx, src/components/PaymentTransactions.tsx, src/components/PostPayment/Success/OrderSummary.tsx | why: billing/payment-history is not part of the dashboard feature, so it gets its own top-level route.
2026-06-14 — Fixed the invoice print output including the whole page (global Nav + Footer) instead of just the invoice card: wrapped `<AppNavbar />` and `<Footer />` in root layout in `print:hidden` divs (both are positioned/structured independently of this wrapper, so it doesn't affect normal layout). | scope: src/app/layout.tsx | why: the invoice page's own `print:hidden` only covered its local action buttons — the site-wide nav/footer rendered by the root layout were still printing on every page, including `/billing/invoice/[transactionId]`.
2026-06-14 — Made course enrollment date cards span the full sidebar row when a row would otherwise contain a single card, and removed the unused `isPrebookingMode` prop from `EnrollmentInfo`. | scope: src/features/course-details/components/EnrollmentInfo.tsx, src/app/courses/[id]/page.tsx, .claude/memory.md | why: avoid the empty half-row gap on `/courses/[id]` when enrollment dates are odd in count.
2026-06-14 — Restyled the course countdown block with a modern glass card, tighter hierarchy, and responsive four-tile layout while removing hardcoded hex colors. | scope: src/features/course-details/components/CountdownTimer.tsx, .claude/memory.md | why: make the enrollment deadline timer feel more polished and aligned with the course details card aesthetic.
2026-06-14 — Renamed the study-plan tab/header to `সিলেবাস` and removed the stats strip from that section so the content starts directly with the chapter list. | scope: src/features/course-details/components/StudyPlanTab.tsx, src/features/course-details/components/CourseTabs.tsx, src/app/courses/[id]/page.tsx, .claude/memory.md | why: simplify the section header and match the requested syllabus presentation.
2026-06-14 — Changed the default selected tab on course details to Instructor instead of Syllabus. | scope: src/app/courses/[id]/page.tsx, .claude/memory.md | why: make the instructor content visible first on `/courses/[id]`.
2026-06-14 — Reworked the instructor tab into a compact scan-friendly list with smaller avatars, lighter cards, and no decorative background blob. | scope: src/features/course-details/components/InstructorTab.tsx, .claude/memory.md | why: make the instructor section easier to skim while keeping the same instructor data.
2026-06-14 — Revamped the `/billing` page UI: rewrote `PaymentSummary`, `PaymentTransactions`, `PaymentCourses`, `PaymentBundles` to use MathPro emerald-token theme (`bg-linear-to-r from-primary to-primary/85`, removed `bg-purple`/raw `bg-gradient-to-r`), full Bengali copy (incl. "কম্বো" for bundles per copy invariant), `englishToBanglaNumbers` for all counts/currency, and fixed course/combo card links to `/dashboard/{id}` + `/dashboard/bundle/{id}` (continue learning) and `/courses/{url||id}` + `/combos/{url||id}` (details) instead of the broken `/course/{id}` and `/bundle/{id}`. Also translated the billing page's error state to Bengali. | scope: src/components/PaymentSummary.tsx, src/components/PaymentTransactions.tsx, src/components/PaymentCourses.tsx, src/components/PaymentBundles.tsx, src/app/billing/page.tsx | why: previous billing page used English copy, hardcoded gradients/legacy `bg-purple` (forbidden token), low-contrast header text, and dead course/bundle links — didn't match MathPro's theme or invariants.
2026-06-14 — Replaced emoji icons in the billing page's PaymentSummary stat cards with `react-icons/bs` icons (`BsWallet2`, `BsBook`, `BsBoxSeam`, `BsArrowRepeat`) inside rounded `bg-primary/10` icon badges. | scope: src/components/PaymentSummary.tsx | why: emoji icons looked dated/inconsistent across platforms; matches the icon-badge pattern used elsewhere (e.g. Dashboard CourseCard via react-icons).

2026-06-14 — Removed the ASSIGNMENT module type from the student LMS (backend migration 014 dropped the assignment feature; quiz kept). Deleted the `useAssignment` hook; removed the assignment state/props/render block from `ModulePlayer` and its wiring + `useAssignment` call in the module `page.tsx` (incl. the goToModule ASSIGNMENT branch and stale `taken`/`fetchEvalutedAssignment` deps); dropped `"ASSIGNMENT"` from the `ModuleCategory` union (`_components/types.ts`) and the course-details category union (`_lib/types.ts`); removed the ASSIGNMENT icon/label in `ModuleListItem`, the ASSIGNMENT gating clause in `ModuleNavButtons`, the ASSIGNMENT icon+label branches in `features/course-details/ChapterAccordion`, and the unused `AssignmentSkeleton` (def + barrel re-export). `npx tsc --noEmit` clean. Deliberately LEFT untouched (out of scope, not assignment-authoring): the `notification.type === "ASSIGNMENT"` handling in NotificationItem/NotificationModal/NotificationsPageClient (notification feature, `type: string`), and the `countAssignmentsAndVideos`/`countModulesAssignmentsVideos` helpers (general counters; their `assignmentCount` is now always 0 and never displayed — removing would ripple return types for no behavioral gain). | scope: src/app/course/[courseId]/[chapterid]/[moduleid]/** , src/features/course-details/** , src/components/Skeletons/** | why: track backend migration 014 — assignment feature removed product-wide.

2026-06-14 — Phase 4 Live-Class toggle (Frontend). Added `live_status`/`live_meeting_id`/`live_meeting_pass`/`live_scheduled_at` to the module `CourseModule` type and a live banner in `ModulePlayer` shown when `live_status==='LIVE'` (Bengali copy: "লাইভ ক্লাস চলছে" + zoom meeting id/passcode/scheduled time). When the class ends (status ENDED / not live) the module plays as a normal video — the banner just disappears. Fields flow from backend `module.js` list/get `SELECT m.*` → course.getFull, no extra fetch. NOTE: pinning live modules to the public homepage is NOT done — `landing-page.tsx` is a static marketing mockup (its "LIVE" badge is decorative), so homepage live-pinning is a separate feature. | scope: src/app/course/[courseId]/[chapterid]/[moduleid]/_components/{types.ts,ModulePlayer.tsx} | why: enrolled students see zoom join info while a module is live; recording shows after.

2026-06-14 — Removed CODE module type (Frontend), tracking backend CODE removal. Dropped CODE from both category unions; removed the two CODE render paths in `ModulePlayer` (internal problem page + Codeforces verify) + the `cfHandle/setCfHandle/checkCFStatus` props (and orphaned `Link` import); removed the `checkCFStatus` callback + CF props in module `page.tsx`; removed CODE from `ModuleListItem` (label+icon), `ModuleNavButtons` gating, `features/course-details/ChapterAccordion` (icon+label), and the displayed "টি কোডিং চ্যালেঞ্জ" count chip in BOTH accordions; removed now-unused `is_cf`/`cf_name` from `ModuleData`. `npx tsc --noEmit` clean. LEFT untouched: helpers' `codeCount`/`totalCodes` tallies (always 0 now, general counters — same call as the assignment counts), and the Ranking "Codeforces অ্যাকাউন্ট" leaderboard column (separate feature, students' CF handles — NOT the CODE module type). | scope: course module player + features/course-details + helpers | why: CODE module type removed product-wide. |
2026-06-15 — Added a live-class widget to the main `/dashboard` page (above StatsSection). New hook `src/hooks/useMyLiveModules.ts` calls the new backend `GET /user/module/live` (aggregates `module.live_status IN ('SCHEDULED','LIVE')` across all enrolled courses); new component `src/components/Dashboard/LiveClassBanner.tsx` renders the first result as a featured card (red for LIVE, primary for SCHEDULED, Bengali copy "এখন লাইভ"/"আসন্ন লাইভ ক্লাস"), linking to `/dashboard/{course_id}`. Named `useMyLiveModules` (not `useLiveClasses`) to avoid collision with the existing per-course `useLiveClasses` hook, which targets the separate legacy `live` table via `/user/live/list/:id` (used by `LiveClassesSection` on the course dashboard) — these are two distinct live-class systems; this widget only covers the new `module.live_status` system. | scope: src/hooks/useMyLiveModules.ts (new), src/components/Dashboard/LiveClassBanner.tsx (new), src/app/dashboard/page.tsx | why: students asked to see/join the current or next live class right from the main dashboard.
2026-06-15 — Reworked LMS quiz behavior to use backend-authored quiz metadata: weighted scoring from per-question `points`, one-time final submission with no retake path, local restore of submitted answers/results, optional timer enforcement from `quiz_time_limit`, and markdown-image rendering so admin-uploaded quiz images display in the student player. | scope: src/app/course/[courseId]/[chapterid]/[moduleid]/**, src/components/SafeHtmlRenderer.tsx, .claude/memory.md | why: align the student quiz player with admin-authored marks/time rules and support image-based MCQ content without duplicate submissions.
2026-06-15 — Quiz submission/attempt state now backend-owned (was localStorage). useQuiz: submitQuiz is async, POSTs answers to `/user/module/quiz/:id/submit` (server grades + returns verdict/score); a module-load effect GETs `/user/module/quiz/:id/attempt` to restore submitted answers/verdict/score so reload + other devices show the same reveal. useQuizTimer stripped of submission persistence/restore — now countdown-only (localStorage `quiz_timer_*` keeps just startTime/totalTime to resume a timed quiz); takes `alreadySubmitted` flag to skip the timer when the server says submitted, and calls bare `onExpire()` on timeout (auto-submit). page.tsx: removed restoreFromTimer/persistSubmission/restoredSubmission wiring. NOTE: ModulePlayer answer-reveal + explanation still decrypt client-side with NEXT_PUBLIC_SECRET_KEY_QUIZ — that key in .env (2wdI…) does NOT match the admin encryptor key (e1031…); backend grades with the admin key. If reveal shows garbage, frontend key needs correcting to the admin key. | scope: _hooks/useQuiz.ts, _hooks/useQuizTimer.ts, [moduleid]/page.tsx | why: "perfect backend-based solution" not localStorage for one-attempt detection + answer reveal.
2026-06-15 — Removed noisy debug `console.log` output from the main `/dashboard` course filter hook. | scope: src/components/Dashboard/DashboardPage/useDashboardFilters.ts | why: keep the dashboard production logs clean without changing filter behavior.
2026-06-15 — Course dashboard now auto-redirects to login on an expired/invalid session instead of showing the dead-end "Error Loading Course" card. Backend returns 200 + {success:false, error:"Your session has expired…" | "…is invalid…"} for bad tokens (vs. "…please enroll" for not-enrolled). useCourseDashboard.ts: when error text matches /session has expired|session is invalid/i it throws an Error tagged `authExpired=true` and exposes `authExpired` from the hook. dashboard/[courseId]/page.tsx: an effect calls the new `redirectToLogin()` helper when `authExpired`, and the error-card render is skipped (shows skeleton) while redirecting. New helper `redirectToLogin()` in helpers/index.ts clears token (localStorage+cookies) + emits tokenUpdate, then `window.location.href = createLoginRedirectUrl()` (returns to current page post-login) — distinct from logout() which reloads in place. Not-enrolled errors deliberately do NOT redirect (re-login won't help). Verified live against all backend cases. | scope: src/hooks/useCourseDashboard.ts, src/app/dashboard/[courseId]/page.tsx, src/helpers/index.ts | why: expired tokens were dumping users on a dead-end card; depends on backend auth_error message split (Math_Pro_backend, same day).
2026-06-16 — Revamped `/profile` UI to a simpler, flatter, modern look. Removed the gradient hero + blur blobs (now a plain h1/subtitle), replaced nested rounded-3xl/border/bg-background/40 sub-boxes with flat sections separated by `border-t border-border` dividers. Unified all fields to one shared `fieldClass` (h-12, rounded-lg, text-sm) so text inputs, selects, and the address textarea share identical box sizing regardless of type (textarea is h-24, non-resizable). Per user follow-up: removed the read-only "লগইন" field from the UI entirely (no longer rendered anywhere — phone/email remain; email read-only), moved গ্রুপ-adjacent লিঙ্গ (gender) out of the অভিভাবক section into মৌলিক তথ্য (it's the student's gender, paired with email), and deleted the right-column "দ্রুত সারাংশ" quick-summary card (password card is now the only right-column section). | scope: src/app/profile/page.tsx | why: user asked for simpler/sleek UI with consistent field sizing, then to hide login, correct gender ownership, and drop the summary.
2026-06-16 — Restored centralized route protection in `src/middleware.ts` (was documented in conventions.md but the file only existed at repo-ROOT `middleware.ts`, which Next IGNORES when a `src/` dir exists — so it never ran, and protection had silently fallen back to copy-pasted per-page `useEffect`+isLoggedIn() guards; that's why `/dashboard` flaked into a login redirect while `/billing` worked). New middleware is cookie-based (edge can't read localStorage or the `#token=` hash), validating the `token` cookie the same way helpers/checkTokenValidity does (identity claim + non-expired exp). Protects /dashboard, /course, /profile, /billing, /ranking and redirects logged-in users away from /auth/*. Per-page client guards intentionally KEPT as the thin fallback for hash/localStorage-only sessions (the case middleware can't see). Deleted the stale tracked root middleware.ts. NOTE: cookie is set on AUTH_COOKIE_DOMAIN=`.mathpro.org` — if prod frontend is served from a non-`*.mathpro.org` host the browser drops the cookie and middleware would over-redirect; verify prod host. User chose `middleware.ts` over Next 16's new `proxy.ts` convention (middleware still works, deprecated/edge runtime). Build confirms it loads. | scope: src/middleware.ts (new), root middleware.ts (deleted) | why: centralize auth so the /dashboard-vs-/billing redirect inconsistency and future auth bugs are fixed in one place.
2026-06-16 — Fixed `/dashboard/[courseId]` page: (1) FeedbackCard rethemed from undefined `purple`/`bg-background`/broken `text-heading dark:text-darkHeading` tokens to `primary`/`bg-card`/`text-foreground` + added standard top-gradient hairline to match sibling cards; (2) CommunityCard heading translated to Bengali ("প্রাইভেট ফেসবুক গ্রুপে যোগ দাও!"), made `communityLink` optional + added early-return null when absent (hides the card), removed the hardcoded `codervaicommunity` fallback from the call site so the section only appears when the API provides a group URL; (3) removed the "গুরুত্বপূর্ণ বার্তা দেখুন" section and its orphaned `Link` import from the page. tsc + eslint clean on touched files. | scope: src/components/Dashboard/CourseDashboard/FeedbackCard.tsx, CommunityCard.tsx, src/app/dashboard/[courseId]/page.tsx | why: review section used off-theme purple tokens; group card heading was English and showed a hardcoded link regardless of API data; barta section was removed per user request.
2026-06-16 — Fixed auth cookie being dropped on non-`*.mathpro.org` hosts (the over-redirect predicted in the prior middleware entry). On `math-pro-mu.vercel.app` `setCookieWithDomain` was always emitting `domain=.mathpro.org`, which the browser rejects because the page isn't under that domain → no `token` cookie → middleware treated every visit as logged-out and bounced protected routes to /auth/login (login itself, and hand-pasted localStorage tokens, both failed the gate). New helper `canUseCookieDomain(domain)` returns false on localhost/127.0.0.1 and whenever the current hostname isn't `bare` domain or `.bare` subdomain; `setCookieWithDomain` now writes a host-only cookie (no domain attr) in those cases and only uses `domain=.mathpro.org` when actually on mathpro.org/subdomain. Prod mathpro.org behavior unchanged. Delete/logout path needed no change — it already clears both a host-only `deleteCookie` and the domain cookie. Verified tsc + eslint clean on the touched file. | scope: src/helpers/index.ts | why: protected routes were unreachable on the Vercel preview (and any non-mathpro.org host) because the auth cookie never persisted.
2026-06-16 — Updated the student coupon service contract to match the hardened backend: `applyCoupon()` no longer sends `original_price` to `/user/coupon/apply`, and the shared `Coupon` type now treats `code` as optional so sanitized public coupon payloads (`active_coupons`) type-check cleanly. No admin changes were needed because managerial coupon APIs were unchanged. | scope: src/services/couponService.ts | why: backend now prices coupons server-side and strips public coupon codes from course/combo detail payloads.
2026-06-16 — `/profile` follow-up: removed the গ্রুপ (group: Science/Arts/Commerce, labeled বিভাগ/ডিপার্টমেন্ট in UI) select per user request; স্কুল/কলেজ is now full-width in its row. Cleaned the orphaned `group` from ProfileFormState, emptyProfileForm, the populate effect, and — deliberately — from the PUT `/user/profile` payload entirely (not sending null) so dropping the field doesn't overwrite any stored backend value. | scope: src/app/profile/page.tsx | why: user said the department/group field isn't needed.
2026-06-16 — Split checkout data more explicitly into profile vs. shipping domains. `CheckoutModal` now uses shared defaults helpers for profile-backed student info and separate shipping defaults, pre-fills the optional book-delivery form from profile `name/phone/address`, adds a one-click "প্রোফাইলের তথ্য ব্যবহার করো" action, and labels shipping as order-only data that is not saved back to profile. Profile update payload remains unchanged; shipping continues to go only through the book order flow. `npx tsc --noEmit` clean. | scope: src/components/CheckoutModal.tsx | why: keep reusable student data in profile while treating book delivery details as separate per-order fulfillment data.
2026-06-16 — Narrowed the checkout profile form to the minimum requested fields only: `name`, `phone`, `schoolCollege`, and `classLevel`. Removed email and all other profile fields from the checkout UI/validation/update payload so checkout no longer edits broader profile metadata; optional book shipping remains separate. `npx tsc --noEmit` clean. | scope: src/components/CheckoutModal.tsx | why: user only wants the small reusable subset on checkout while keeping the full profile page as the place for all other profile data.
2026-06-16 — Course payment success redirects now go directly to the billing invoice route using the transaction ID: the live SSLCommerz success callback sends course purchases to `/billing/invoice/:transactionId` (bundle success still goes to the post-payment success page), and the course purchase email CTA/reconciliation CTA also point to that invoice URL. Fallback remains the old course success page if SSLCommerz does not provide `tran_id`. | scope: Math_Pro_backend/controllers/user/payment.js, .claude/memory.md | why: user wants the student to land on the invoice immediately after a successful course purchase.
2026-06-16 — Extended the same invoice-first success redirect rule to bundle purchases: successful bundle payments now also redirect to `/billing/invoice/:transactionId`, and bundle email/reconciliation CTAs use that invoice URL too. Old bundle post-payment success page remains only as fallback when `tran_id` is absent. | scope: Math_Pro_backend/controllers/user/payment.js, .claude/memory.md | why: keep post-payment behavior consistent between course and bundle purchases.
2026-06-16 — Removed client-supplied price from course checkout: `buyCourse` no longer sends `eventId: courseData.price * 6251`. The backend now derives course/bundle price authoritatively from the DB, so the client must not send a price (it was an arbitrary-price vulnerability). `npx tsc --noEmit` clean. | scope: src/hooks/useCourseDetails.ts | why: pairs with the backend fix that ignores `eventId`; bundle checkout already sent no price.
2026-06-18 — Treated announcement notifications as rich-body notifications in the notification modal/item flows and let the notifications page prefer `data.html` when present; the dashboard announcement card was already consuming HTML safely, so it needed no change. | scope: src/components/NotificationModal.tsx, src/components/NotificationItem.tsx, src/app/notifications/_components/NotificationsPageClient.tsx | why: announcement notifications now share the backend notification/announcement contract and should render the same rich HTML as the dashboard announcement section.
2026-06-18 — Added an explicit announcement badge/icon in the notification list and modal rows so announcement notifications are visibly distinguished from other rich-body notifications. | scope: src/components/NotificationModal.tsx, src/components/NotificationItem.tsx | why: make course announcements readable at a glance without changing the read/expand behavior.

2026-06-18 — Centralized cross-subdomain auth (Option B): student Frontend is the single login UI for both apps. Switched AUTH_COOKIE_DOMAIN to .mathpro.academy (was .mathpro.org). Added helpers handlePostLoginRedirect/getTokenType/isAllowedRedirectTarget: after login (password + Google) the token is persisted to the shared cookie, then the user is routed by JWT `type` — admins/moderators (type 1/2) go to NEXT_PUBLIC_ADMIN_URL (default https://admin.mathpro.academy) with the token in the `#token` hash (open-redirect guard limits hash hand-off to same-origin or *.mathpro.academy/localhost); students (type 3) keep same-origin /dashboard routing. The already-logged-in effect also routes via the helper so an authenticated admin landing on /auth/login is forwarded. | scope: src/helpers/index.ts, src/app/auth/login/page.tsx | why: one auth surface; admin app has no login form and redirects here. Pairs with mathpro_admin changes (login redirector, captureTokenFromUrl, middleware bounce).

2026-06-18 — Fixed handlePostLoginRedirect sending admins to prod (https://admin.mathpro.academy) during local dev: isAllowedRedirectTarget rejected http://localhost:2026 (it only allowed same-origin or https *.mathpro.academy), so the admin branch fell back to NEXT_PUBLIC_ADMIN_URL. Now localhost/127.0.0.1 (any port, any protocol) is explicitly allowed as a redirect target; non-localhost still requires https + *.mathpro.academy. | scope: src/helpers/index.ts (isAllowedRedirectTarget) | why: local dev SSO admin login (frontend :3000 → admin :2026) must honor the localhost redirect param instead of bouncing to production.
2026-06-19 — Made the `/courses` featured hero slider responsive across phones and tablets by tightening the frame ratio, scaling copy/padding, and relocating controls so the card no longer feels cramped on narrow screens. | scope: src/features/courses-page/components/FeaturedCourseSlider.tsx | why: keep the courses landing hero usable on mobile and tablet without changing the slider content or behavior.
2026-06-19 — Tightened the `/courses` slider mobile layout further by moving the slide copy into an inset panel and constraining the content width so the hero text no longer spills toward the right edge on phones. | scope: src/features/courses-page/components/FeaturedCourseSlider.tsx | why: address the remaining horizontal overflow/edge spill seen in the mobile screenshot.
2026-06-19 — Nudged the `/courses` slider nav arrows farther toward the card edges on mobile so they no longer overlap the blurred content block as much. | scope: src/features/courses-page/components/FeaturedCourseSlider.tsx | why: preserve the featured badge and CTA visibility while keeping the arrows reachable on phones.
2026-06-19 — Made `/billing` mobile-first: narrowed the page shell padding on phones, reduced billing section header/body padding, and forced transaction/course/combo action buttons to remain one-per-row full width until `md` so the cards don't compress into side-by-side button rows on phones. | scope: src/app/billing/page.tsx, src/components/PaymentSummary.tsx, src/components/PaymentTransactions.tsx, src/components/PaymentCourses.tsx, src/components/PaymentBundles.tsx | why: user wanted the billing page fully responsive on phones with consistent button sizing and stacked layout.
2026-06-19 — Removed the decorative emoji icon blocks from the empty-state course and combo cards in `/billing` so the cards read cleaner on mobile. | scope: src/components/PaymentCourses.tsx, src/components/PaymentBundles.tsx | why: user asked not to show the icon on course or combo cards.
2026-06-19 — Removed the left-side transaction icon from `/billing` transaction rows so course/combo cards keep the same layout without the emoji marker beside the title. | scope: src/components/PaymentTransactions.tsx | why: user confirmed the visible icon should be removed from the actual course/combo card header row.
2026-06-19 — Made `/dashboard/[courseId]` fully responsive on mobile: reordered sections for mobile (Routine → Streak → Progress → Live → Announcements → Ranking → Community → Feedback last), added sm/lg breakpoints to all 8 dashboard cards (padding, text sizes, icon sizes, gaps, border-radius), used hidden/lg:hidden to show right-column cards inline on mobile while keeping the 2-col layout on desktop. Fixed Tailwind v4 class warnings (bg-gradient-to-r → bg-linear-to-r, !h-auto → h-auto!). | scope: page.tsx + all CourseDashboard/ components (RoutineHero, StreakCountCard, ProgressCard, LiveClassesSection, AnnouncementsCard, RankingCard, CommunityCard, FeedbackCard) | why: user reported the dashboard was not fluid on phones; sections needed reordering for mobile UX.
2026-06-19 — Added a navbar notification badge that aggregates counts across enrolled courses and clears when the bell is opened. | scope: src/components/Nav.tsx | why: surface notification state at the top-level nav instead of only inside the notifications page.
2026-06-19 — Switched the navbar badge to true unread state (`is_read = false`) and broadcast a `notificationUpdated` event from read actions so the badge drops immediately after a notification is read. | scope: src/components/Nav.tsx, src/components/NotificationModal.tsx, src/components/NotificationItem.tsx, src/app/notifications/_components/NotificationsPageClient.tsx | why: the badge should reflect unread notifications, not bell-open state.
2026-06-19 — Removed the notification page header summary stats card so the page shows only the hero text and the notification list/actions. | scope: src/app/notifications/_components/NotificationsPageClient.tsx | why: user asked to remove that card from the notifications page.
2026-06-19 — Trimmed redundant copy from the merged notifications hero by removing the page subtitle and the list helper line, leaving only the title, the read-all action, and the list. | scope: src/app/notifications/_components/NotificationsPageClient.tsx | why: user asked to remove redundant information after merging the two cards.
2026-06-19 — Made the notifications page title responsive on phones by reducing the mobile heading size and capping its width so it wraps cleanly. | scope: src/app/notifications/_components/NotificationsPageClient.tsx | why: the hero title was breaking on small screens.
2026-06-19 — Moved the "সাম্প্রতিক আপডেট" label into the notifications hero and renamed the list section header to "নোটিফিকেশন" so the page no longer repeats the same heading twice. | scope: src/app/notifications/_components/NotificationsPageClient.tsx | why: user wanted the recent-updates label in the hero instead of the lower section.
2026-06-19 — Removed the small "MathPro আপডেট" hero label and forced the notifications hero title onto one line with a smaller mobile font size. | scope: src/app/notifications/_components/NotificationsPageClient.tsx | why: user wanted the recent-updates title to stay on one line and not split on phones.
2026-06-19 — Restored the small "MathPro আপডেট" hero label and removed the redundant "নোটিফিকেশন" heading above the সব পড়া হয়েছে button. | scope: src/app/notifications/_components/NotificationsPageClient.tsx | why: user clarified the badge should stay and only the lower section title should go away.
2026-06-19 — Moved the navbar desktop switch from `md` to `lg` so tablets use the phone-style menu and actions instead of the desktop nav row. | scope: src/components/Nav.tsx | why: user wanted tablet behavior to match mobile.
2026-06-19 — Replaced the courses-page instructor slider with the shared `AboutSection` so the page now uses the reusable `আমার সম্পর্কে` / `আমাদের সম্পর্কে` block instead of a separate teacher section. | scope: src/app/courses/page.tsx, src/features/courses-page/components/index.ts | why: user asked to remove the instructor section and reuse the existing about section.
2026-06-19 — Constrained the course card price pill with `w-fit self-start` so it sizes to its content instead of stretching full-width on mobile. | scope: src/features/courses-page/components/LandingStyleCourseCard.tsx | why: the price badge was filling the card width.
2026-06-19 — Right-aligned the course card price pill with `self-end` so the content-sized badge sits on the card's right side in the stacked mobile layout. | scope: src/features/courses-page/components/LandingStyleCourseCard.tsx | why: user wanted the pill on the right, not the left.
2026-06-19 — Added a shared combo card component and wired the courses page bundle cards to it, while filtering bundles by inherited child-course/category tags so combos appear alongside matching courses. | scope: src/features/courses-page/components/ComboCard.tsx, src/features/courses-page/components/PremiumBundleCard.tsx, src/features/courses-page/hooks/useCoursesPage.ts, src/features/courses-page/_lib/{tagUtils.ts,types.ts}, src/app/combos/page.tsx, src/helpers/index.ts | why: keep combo presentation consistent across the courses and combos pages and let combo cards participate in course/tag filters.
2026-06-19 — Redesigned the shared combo card to match the course card layout, show combo thumbnails when available, and compute the crossed-out original price from child-course `x_price` values instead of a separate count/savings block. | scope: src/features/courses-page/components/ComboCard.tsx, src/features/courses-page/components/PremiumBundleCard.tsx, src/app/combos/page.tsx, src/features/courses-page/_lib/types.ts | why: make combos feel like first-class course cards while keeping the price semantics accurate.
2026-06-19 — Removed the combo course roster from the shared card so combo tiles now read like plain course cards, using only the combo short description when present. | scope: src/features/courses-page/components/ComboCard.tsx | why: user wanted the combo card on the courses page to stop showing the course list and match the course card surface more closely.
2026-06-19 — Standardized combo thumbnails on `bundle_thumb_4_3` across combo cards and combo list types, while keeping the combo detail page priority on `intro_video` and falling back to that same thumbnail. | scope: src/features/courses-page/components/ComboCard.tsx, src/features/courses-page/_lib/types.ts, src/app/combos/page.tsx, src/app/combos/[id]/page.tsx | why: admin now uploads one shared bundle thumbnail field and the student UI should consume the same key consistently.
2026-06-19 — Changed course details FAQs to use the course-specific `faq_list.faqs` first and fall back to public FAQs only when the course has none. | scope: src/app/courses/[id]/page.tsx, src/features/course-details/components/CourseDetailsTab.tsx | why: user wanted course FAQs to take priority over global FAQs on the course detail page.
2026-06-29 — Replaced the navbar brand placeholder (emerald box with literal "M") with the actual logo image via next/image. | scope: src/components/Nav.tsx | why: use the real MathPro logo (public/assets/logo-removebg-preview.png) in the navbar instead of the "M" stand-in.
2026-06-29 — Navbar logo polish: enlarged the logo mark (size-11 md:size-14), split the wordmark into MATH (foreground/white per scroll state) + PRO (emerald), and set the logo as the favicon via src/app/icon.png (removed the old src/app/favicon.ico). | scope: src/components/Nav.tsx, src/app/icon.png, src/app/favicon.ico (removed) | why: match the requested two-tone MATH PRO wordmark, larger brand mark, and use the real logo as favicon.
2026-06-29 — Switched the navbar MATH PRO wordmark to the Michroma Google font to match the typography baked into public/assets/math pro.png (wide geometric all-caps with flat-cut A apex). Wired via next/font/google in layout.tsx, registered --font-michroma in globals.css @theme, applied font-michroma on the wordmark and dropped the Manrope-era tracking/hover-tracking classes (Michroma is already wide). | scope: src/app/layout.tsx, src/app/globals.css, src/components/Nav.tsx | why: brand wordmark font should match the logo art.
2026-06-29 — Logo wordmark font finalized: replaced Michroma (single 400 weight, too light) with Orbitron 700/800/900 to match the bold geometric MATH PRO in the logo art; renamed the CSS var --font-michroma -> --font-logo (layout.tsx + globals.css + Nav.tsx) and applied font-logo font-black at text-xl md:text-3xl. | scope: src/app/layout.tsx, src/app/globals.css, src/components/Nav.tsx | why: brand wordmark needed to be bolder and larger to match the logo image.
2026-06-29 — Perf Stage 1: migrated usePublicTestimonials + usePublicInstructors from raw useEffect+axios to React Query (keys ["public-testimonials"], ["instructors"] — the latter now shares cache with useCoursesPage), removed the mounted-gate in useCourseDirectory, and lazy-loaded below-the-fold landing sections (TestimonialMarquee/FAQSection/AboutSection via next/dynamic ssr:false). Deleted dead src/hooks/useAllCourses.ts (0 consumers, dup of ["courses"]). | scope: src/hooks/usePublicTestimonials.ts, src/hooks/usePublicInstructors.ts, src/hooks/useCourseDirectory.ts, src/components/landing-page.tsx, src/hooks/useAllCourses.ts (removed) | why: stop redundant per-navigation refetches across home/courses/combos and trim landing-page initial JS.
2026-06-29 — Perf Stage 2: made the 3 public pages (/, /courses, /combos) server components that prefetch public catalog endpoints with fetch({next:{revalidate:300}}) and hand them to the existing client bodies via <HydrationBoundary>. Added src/lib/catalog-server.ts (server-only QueryClient prefetch helper, keyed identically to the client hooks); renamed page bodies to CoursesPageClient/CombosPageClient. Build now marks all three as ○ Static w/ 5m ISR (were dynamic). Chose HydrationBoundary over app-wide cacheComponents to keep it route-scoped — authed pages + middleware untouched. | scope: src/app/page.tsx, src/app/courses/page.tsx (new server wrapper) + CoursesPageClient.tsx, src/app/combos/page.tsx (new server wrapper) + CombosPageClient.tsx, src/lib/catalog-server.ts | why: real first-paint content + fewer backend hits on public pages.
2026-06-29 — Perf cross-cutting: next.config.ts — added images.formats ['image/avif','image/webp'] and experimental.optimizePackageImports ['framer-motion','@fortawesome/react-fontawesome'] (lucide-react/@headlessui/react/react-icons are optimized by Next defaults already). remotePatterns left as wildcard pending the real prod image host. | scope: next.config.ts | why: smaller catalog thumbnails + tree-shake heavy barrels not covered by defaults.
2026-06-29 — Wired landing-page hero CTAs and class-category cards to the real `/courses` route, and taught the courses page to honor an incoming `?category=` query by matching the existing filter tabs on first load. | scope: src/components/landing-page.tsx, src/app/courses/CoursesPageClient.tsx, src/features/courses-page/hooks/useCoursesPage.ts | why: landing buttons were not taking students to the intended course listings.
